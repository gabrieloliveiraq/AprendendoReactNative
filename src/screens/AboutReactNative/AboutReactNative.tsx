import { ScrollView, View } from "react-native";

export function AboutReactNative() {
  return (
    <ScrollView
      style={{ backgroundColor: "#f2f2f2", paddingTop: 40, padding: 8 }}
    ></ScrollView>
  );
}
